__pycache__/
*.log
*.pyc
*.ipynb_checkpoints
.env
.vscode/
.idea/
# ─── Terraform ───────────────────────────────────────────
terraform/.terraform/        # provider cache
terraform/*.tfstate*         # state files & backups
terraform/*.tfvars           # personal or env‑specific vars
terraform/*.auto.tfvars      # auto‑loaded var files
terraform/crash.log          # crash logs
terraform/*-override.tf*     # local overrides
*.tfplan                      # plan output files
*.pem *.key *.crt            # any keys/certs lying around
# ─── Extra ignores we missed ───────────────────────────
# Docker build artefacts
docker/
*.tar

# ML model binaries & temp images
ml_model/*.pkl
ml_model/*.tar
ml_model/image.tar

# Python noise
__pycache__/
*.py[cod]

# VSCode / env files (optional)
.vscode/
.env
terraform/**/.terraform/
terraform/**/terraform.tfstate*

# Terraform secrets
*.tfvars
*.auto.tfvars
*.tfvars.json
terraform.tfstate*
.terraform/
crash.log
