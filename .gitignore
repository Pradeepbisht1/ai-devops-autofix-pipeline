# === Node / frontend ===
dashboard/node_modules/
dashboard/dist/
*.log

# === Python ===
**/venv/
*.pyc
__pycache__/
app/src/venv/
ml_model/venv/
*.egg-info/

# === Terraform ===
*.tfstate
*.tfstate.backup
.terraform/
terraform.tfvars     # if it has secrets (or keep a separate example file)
temp.yaml           # if this is generated with sensitive values

# === Model artifacts (you fetch from S3/workflow) ===
ml_model/models/model.tar.gz
model.tar.gz
ml_model/prom_features.csv

# === Secrets / envs ===
*.env
*.secret
credentials.json
aws_credentials
# if you use some config with keys
*.key
*.pem

# === Git LFS metadata (auto if you track with LFS) ===
.gitattributes

# === OS / editor noise ===
.DS_Store
Thumbs.db
*.swp
*.swo

# === Misc ===
coverage/
htmlcov/
ml_model/models/model.pkl
terraform/terraform.tfvars
dashboard/node_modules/
